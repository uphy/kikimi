# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Product Spec (single source of truth)

プロダクト仕様の正は [`kikimi.md`](kikimi.md) にある。全体像・不変条件・データモデル・LLM 整形パイプライン・
Watcher 仕様などが書かれており、設計判断に迷ったら必ず読む。
機能単位の詳細設計は `docs/design/*.md` に書く（実装前に必ず読む／起草する）。
開発方式・移行計画の詳細は [`docs/development-process.md`](docs/development-process.md)（実装サイクルや workflow の運用を確認するときに読む）。

## Overview

Kikimi（聞き耳）は Chirami から会議書き起こし機能を切り出した、会議特化のリアルタイム書き起こし macOS アプリ。
メニューバー常駐（LSUIElement）+ フローティング NSPanel。システム音声とマイクを別ストリームで sherpa-onnx により
オンデバイス書き起こしし、Claude Haiku で整形・サマリ更新・Watcher 実行を行う。

## 参考リポジトリ（Chirami）

Kikimi は先行プロジェクト Chirami（非公開）から会議書き起こし機能を切り出したもの。ローカルに並列クローンが
ある環境での参照ルール・場所・詳細マップは `CLAUDE.local.md` を参照。
**Chirami のコードは read-only 参照専用。絶対に修正しない。**

## 開発方式: Vibe Coding

Claude Code が「詳細設計 → セルフレビュー → 実装 → 実装レビュー → 単体テスト」を
**Dynamic Workflow（`.claude/workflows/phase-cycle.js`）で自律的に回す**。詳細は
[`docs/development-process.md`](docs/development-process.md) 2章。

ユーザーが介入するのは以下のみ:

- Phase 開始時の目標合意
- 詳細設計レビュー後の Go/No-Go
- Phase 完了時の動作確認
- ロードマップ調整

それ以外は自走する。判断に迷ったら妥当な選択をして進め、Phase 完了時に判断根拠を報告する。

**UI 動作確認は基本的にユーザーが行う。`kikimi-verify` skill はユーザーから明示的に指示されたとき
（「動作確認して」や `kikimi-ui-verify` workflow の実行指示）のみ実行する**（詳細は `CLAUDE.local.md` 参照）。
phase-cycle / fix-cycle workflow には UI 検証ステップを含めない。

## Build

```bash
# Initial setup (requires xcodegen, cmake)
mise install
mise run generate
mise run signing-identity   # 初回のみ。TCC 権限を再ビルドで失わないため（下記）

# Build and run in Xcode (project file generated by xcodegen; do not edit .xcodeproj directly)
open Kikimi.xcodeproj
```

`swift build` でもビルド可能（`Package.swift` あり）。この開発環境には Xcode.app 本体（GUI IDE）がなく
Command Line Tools のみのため、**実際のビルド経路は `xcodebuild` ではなく `swift build`（mise タスク経由）**。
`xcodegen generate` は IDE での閲覧・編集体験のためだけに使う。

### mise Tasks

- `mise run generate` — xcodegen でプロジェクトファイルを生成
- `mise run build` — Release ビルド（`.build/release/Kikimi` → `build/Kikimi.app` バンドル化 → 署名）
- `mise run apply` — `~/Applications/Kikimi.app` にインストール（実行中なら再起動）
- `mise run signing-identity` — ローカル開発用のコード署名 identity を作成（初回のみ・冪等）
- `mise run reset-permissions` — Kikimi の TCC 許可をリセット（署名方式を変えた直後のみ）
- `mise run clean` — ビルド成果物を削除
- `mise run purge` — アプリ・config・state を全削除
- `mise run lint` / `mise run lint-fix` — SwiftLint
- `mise run verify-smoke` — `kikimi-verify` のスモークテスト

### コード署名と TCC 権限

**`mise run signing-identity` を一度実行しないと、ビルドのたびにマイク・画面収録の許可が失効する。**
症状（cdhash 起因で許可が切れる・トグルで直らない等）の詳細と対処は `kikimi-signing-troubleshoot` skill
（`~/.claude/skills/kikimi-signing-troubleshoot/`）を参照。

## アーキテクチャ概要

kikimi.md 13 章に準拠。主要コンポーネント:

| コンポーネント | 役割 |
|---|---|
| `AppConfig.shared` | `~/.config/kikimi/config.yaml` の読み書き |
| `AppState.shared` | `~/.local/state/kikimi/state.yaml` の読み書き |
| `SessionStore.shared` | セッションのファイル I/O（JSONL 追記・meta 更新） |
| `WindowManager.shared` | フローティングパネルの管理 |
| `AudioCapture` | AVAudioEngine（マイク）+ CoreAudio Process Tap（システム音声）ラッパー |
| `SttEngine` | FluidAudio（Nemotron 3.5 ASR Streaming）ラッパー（2 インスタンス） |
| `RefinementQueue` | バッチ整形キュー + Haiku 呼び出し（Phase 2） |
| `SummaryUpdater` | サマリ更新スケジューラ（Phase 2） |
| `WatcherRunner` | Watcher の実行と state 管理（Phase 3） |
| `WikiExporter` | セッション終了時の Markdown export（Phase 3） |

### コードマップ

| ディレクトリ | 役割 |
|---|---|
| `Kikimi/AudioCapture/` | マイク・システム音声の取込（AVAudioEngine / ScreenCaptureKit） |
| `Kikimi/Stt/` | STT エンジン（FluidAudio ラッパー）・接着層（TranscriptPipeline） |
| `Kikimi/Diarization/` | 話者分離（声紋 embedding・リアルタイム帰属判定） |
| `Kikimi/Refinement/` | Haiku バッチ整形（プロンプト構築・マージ・バリデーション） |
| `Kikimi/Summary/` | サマリ state 管理・patch 適用・view レンダリング |
| `Kikimi/Watchers/` | Watcher 定義・実行・schema/view 処理 |
| `Kikimi/Markdown/` | 会話ログの Markdown レンダリング・コピー |
| `Kikimi/LLM/` | Claude CLI/API クライアント・課金集計・スタブプロバイダ |
| `Kikimi/SessionStore/` | セッションのファイル I/O（JSONL・meta・各種 state） |
| `Kikimi/Config/` | `AppConfig` / `AppState` / YAML 読み書き |
| `Kikimi/WikiExport/` | セッション終了時の Wiki 向け Markdown export |
| `Kikimi/Playback/` | セグメント単位の音声再生 |
| `Kikimi/Window/` | NSPanel 管理・メニューバー・URL scheme ルーティング |
| `Kikimi/ViewModels/` | 各ウィンドウの ViewModel |
| `Kikimi/Views/` | SwiftUI 画面（Session Window / List / Settings） |
| `KikimiTests/` | 各ディレクトリに対応する XCTest / swift-testing |

### Config/State Separation

Chirami と同じ流儀（YAMLStore パターン参照）。

- `~/.config/kikimi/config.yaml` — モデル・トリガ・パス参照設定
- `~/.local/state/kikimi/state.yaml` — ウィンドウ位置・可視状態

### データ保存

セッションは `~/.local/state/kikimi/sessions/{ISO8601開始時刻}_{短縮UUID}/` に保存。詳細は kikimi.md 4-5 章。

## テスト方式（docs/development-process.md 2.9）

- **レイヤ 1: XCTest / swift-testing** — 各機能の入出力を対象。実装フェーズと同時に書く
- **レイヤ 2: `kikimi-verify` skill** — 起動 → 録音開始 → ダミー音源投入（`KIKIMI_TEST_INPUT`）→ 停止 →
  セッションフォルダ確認。整形は `KIKIMI_STUB_LLM=1` でスタブ化
- **レイヤ 3: 実戦テスト** — Phase 4 でリアル会議 3 本以上

## Logging Rules

| Situation | Level |
|---|---|
| Debug info | `debug` |
| Successful completion | `info` |
| Misconfiguration or missing resource | `warn` / `.warning` |
| Processing failure or error | `error` |

```bash
log stream --predicate 'subsystem == "io.github.uphy.Kikimi"' --level debug
```
