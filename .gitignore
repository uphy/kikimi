.DS_Store
*.xcuserstate
xcuserdata/
.build/
/build/
DerivedData/
Package.resolved
.swiftpm/
*.xccheckout
*.moved-aside
*.pbxuser
.mise/cache/
CLAUDE.local.md

# Local working notes for interactive review sessions -- never commit
_ux-review-*

# Generated by xcodegen (mise run generate)
Kikimi.xcodeproj/

# One worktree per PR, created by `mise run wt` (docs/development-process.md 2.11). Same location
# Claude Code's own EnterWorktree uses.
.claude/worktrees/

# Byte-code caches from the verification scripts in .claude/skills/
__pycache__/
*.pyc

# LLVM coverage profile dropped in the repo root by `swift test`
*.profraw

# Agent worktrees (`isolation: "worktree"`) -- real git worktrees, never part of this checkout
.claude/worktrees/

# Built by `mise run build:web` from web/ (docs/design/39-webview-markdown.md §9)
Kikimi/Resources/editor/
web/node_modules/

# Local-only reference map for a private sibling repo
docs/references/chirami-map.md
